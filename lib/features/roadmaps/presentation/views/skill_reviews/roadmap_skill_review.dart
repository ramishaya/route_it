import 'package:flutter/material.dart';

class SkillReviewsView extends StatelessWidget {
  const SkillReviewsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("this is the reviews view"),
    );
  }
}